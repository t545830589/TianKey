name: tian_key

description: "Tian Key V11 Mazda Axela BLE Controller App"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.8

  # 当前第一阶段只保留兼容 Flutter 3.22 / Dart 3.4 的本地存储版本
  shared_preferences: 2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
