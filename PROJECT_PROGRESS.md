# Tian Key V11 项目进度

> 进度基准来自用户提供的当前详细进度表；“本批次代码阶段”是对当前仓库静态代码与本次批量写入后的保守评估。最终 APK 是否达到目标图，必须以 Actions 构建成功 + 实机安装截图验收为准。

## 当前详细进度

| V4目标 | 上一版完成 | 本批次代码阶段 | 状态 | 还缺什么 |
|---|---:|---:|---|---|
| 最终 HUD/UI 框架 | 45% | 70% | 🟡 | 实机截图逐像素验收、尺寸/状态栏适配 |
| 目标图片视觉风格 | 40% | 75% | 🟡 | APK 实机验收；发现偏差后集中修正 |
| 车辆背景/素材接入 | 75% | 100% | 🟢 | 目标素材已直接作为页面底图；待 APK 验收 |
| 六项车辆控制模拟 | 80% | 90% | 🟢 | 真实 BLE/ESP32 指令链路 |
| 锁车 | 90% | 90% | 🟢 | 真实硬件联调 |
| 解锁 | 90% | 90% | 🟢 | 真实硬件联调 |
| 寻车双脉冲逻辑表达 | 75% | 75% | 🟡 | 真实 GPIO 脉冲与时序联调 |
| 升窗7秒 | 80% | 80% | 🟢 | 真实 GPIO 保持7秒 |
| 降窗7秒 | 80% | 80% | 🟢 | 真实 GPIO 保持7秒 |
| 后备箱7秒 | 80% | 80% | 🟢 | 真实 GPIO 保持7秒 |
| 管理员初始密码 | 100% | 100% | 🟢 | 无；固定为 13092991951 |
| 管理员认证模拟 | 80% | 85% | 🟢 | 真实 ESP32 持久化认证/设备绑定 |
| 临时借车 | 75% | 80% | 🟢 | 实机 BLE 授权链路 |
| 临时密码有效期 | 70% | 80% | 🟡 | ESP32 时钟/失效联动实机验证 |
| 唯一管理员席位 | 65% | 70% | 🟡 | 真正的设备席位写入、迁移、抢占/恢复规则 |

## 总体

- 上一版用户进度表平均：**74.6%**
- 本批次代码阶段保守评估：**83.2%**
- 本批次对应剩余：**16.8%**
- **注意：83.2% 是代码阶段评估，不等于最终 APK 验收完成度。**

## 本批次一次性推进

1. 主车辆页改为直接使用 `assets/home_controls_bg.png` 作为目标 HUD/车辆/六按钮/底部导航的完整视觉底图。
2. 取消原来与目标图不一致的 5 项底部导航，改为目标图的三项导航：车辆 / 借车 / 设置。
3. 在目标图按钮上增加透明交互热点，不覆盖目标视觉素材。
4. 蓝牙键统一承担：扫描 → 发现后连接 → 已连接后断开。
5. 六项车辆控制全部保留并接入统一权限门控。
6. 借车页直接使用 `borrow_page_bg.png`，并接入临时密码、有效期和借车认证。
7. 设置页直接使用 `settings_page_bg.png`，并接入蓝牙、时间、密码、管理、声音、日志入口。
8. 管理员认证、修改密码、设备名称、授权切换、临时借车、时间同步、恢复出厂、统一日志继续保留。
9. 管理员初始密码保持 **13092991951**。
10. 授权关闭只锁定车辆控制，不清除当前管理员会话；管理员仍可恢复授权。
11. 保留目标弹窗素材并用于管理员认证、修改密码、修改设备名、恢复出厂。

## 尚未完成的核心工作

- 真实 BLE 扫描/连接/断开，而不是模拟延时。
- ESP32 协议与真实 GPIO 指令链路。
- ESP32 保存信息与 APP 状态真正双向同步。
- 时间同步失败、过期、借车失效与 ESP32 实机状态联动。
- 唯一管理员席位的真实设备绑定、迁移和恢复规则。
- 最终 APK 在真实手机上的截图对照与逐项视觉修正。

## 协作方式

- 用户：运行 Actions / 安装 APK / 提供真实错误截图和实机截图。
- 助手：根据实际日志和仓库当前代码审核后批量写入，不根据猜想编造错误。
- 优先批量推进：同一轮集中处理 UI、状态机、权限、借车、日志等相关项，避免一个按钮一个模块地推进。
