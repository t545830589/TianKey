# Tian Key V11 项目进度

> 进度基准继续沿用用户提供的目标与当前仓库实际代码。这里的“代码阶段”只统计仓库已经写入的内容；最终 APK 完成度必须经过 Actions 成功 + 实机安装截图验收，不能用代码百分比替代实机验收。

## 当前详细进度

| V4目标 | 上一批代码阶段 | 本批次代码阶段 | 状态 | 还缺什么 |
|---|---:|---:|---|---|
| 最终 HUD/UI 框架 | 80% | 80% | 🟡 | 实机截图逐像素验收、状态栏/屏幕尺寸适配 |
| 目标图片视觉风格 | 82% | 85% | 🟡 | 实机截图对照；发现偏差后集中修正 |
| 车辆背景/素材接入 | 100% | 100% | 🟢 | 目标素材已直接作为页面底图；待 APK 实机验收 |
| 六项车辆控制模拟 | 95% | 95% | 🟢 | 真实 ESP32 指令帧/GPIO 联调；当前没有虚构为完成 |
| 锁车 | 95% | 95% | 🟢 | 真实 ESP32 指令帧/GPIO 联调 |
| 解锁 | 95% | 95% | 🟢 | 真实 ESP32 指令帧/GPIO 联调 |
| 寻车双脉冲逻辑表达 | 80% | 80% | 🟡 | 真实 GPIO 双脉冲与时序联调 |
| 升窗7秒 | 90% | 90% | 🟢 | 真实 GPIO 保持7秒；APP侧已有7秒状态倒计时 |
| 降窗7秒 | 90% | 90% | 🟢 | 真实 GPIO 保持7秒；APP侧已有7秒状态倒计时 |
| 后备箱7秒 | 90% | 90% | 🟢 | 真实 GPIO 保持7秒；APP侧已有7秒状态倒计时 |
| 管理员初始密码 | 100% | 100% | 🟢 | 固定为 13092991951；实机验证 |
| 管理员认证模拟 | 88% | 92% | 🟢 | ESP32 真实持久化认证/设备绑定 |
| 临时借车 | 88% | 92% | 🟢 | 实机 BLE 授权链路与 ESP32 联动 |
| 临时密码有效期 | 90% | 90% | 🟢 | ESP32 时钟/失效联动实机验证；APP侧到期自动清除 |
| 唯一管理员席位 | 72% | 80% | 🟡 | APP安装级席位已真实持久化；ESP32侧席位迁移/抢占/恢复规则仍待硬件协议 |
| BLE 服务发现基础 | 90% | 97% | 🟢 | 已真实 discoverServices；本批增加真实 GATT service UUID 库存读取与 Services Changed 后重新发现；仍缺 ESP32 协议定义后才能确定读写特征 |
| BLE 已保存设备重连能力 | 0% | 90% | 🟢 | `remoteId` 可直接创建 BluetoothDevice 并走真实连接/服务发现；尚未在目标手机上实机验收 |

## 总体

- 上一批代码阶段：**约 90.0%**
- 本批次代码阶段：**约 90.5%**
- 本批次代码阶段剩余：**约 9.5%**
- **注意：90.5% 仍然不是最终 APK 完成度。** 当前最主要的剩余不是再堆 UI，而是 ESP32 协议/GPIO/持久化真实联调，以及实机视觉验收。

## 最近批次已完成

1. 保持真实 BLE 扫描、连接、断开，不再用 APP 延时伪造“发现设备”。
2. `TianKeyBleService.discoverServices()` 统一保存真实发现到的 `BluetoothService` 列表。
3. 本批次新增 `discoveredServiceUuids`：只读取已连接外设真实返回的 GATT service UUID，用于后续协议核对；没有把任何 UUID 猜成 TianKey 协议。
4. 本批次新增 `reconnectSavedRemoteId()`：使用已经持久化的真实 `remoteId` 创建 `BluetoothDevice`，然后复用真实连接 + 服务发现流程；不通过名称猜设备。
5. 保留 Services Changed 事件后的自动重新发现，避免 GATT 缓存失效后继续使用旧服务列表。
6. Actions 的 Android 构建链继续保持 Java 17、Flutter 3.19.6、BLE 权限和 minSdk 21。
7. 管理员席位继续使用每次安装生成并持久化的安装 ID；保留旧 `PHONE-TIANKY-01` 仅作迁移兼容。
8. 继续保留管理员初始密码 **13092991951**，没有写入错误密码 **13092991954**。
9. 临时借车起止时间、到期自动清除和借车会话撤销继续保留。
10. 车辆六项控制的协议名/GPIO映射继续沿用仓库现有代码；由于当前仓库没有 ESP32 固件或协议帧定义，本批次没有自行编造 BLE service UUID、characteristic UUID 或数据帧。
11. 目标页面继续直接使用仓库目标素材，保持固定宽高比，没有重新绘制一套与目标不同的 UI。

## 尚未完成的核心工作

- ESP32 协议与真实 GPIO 指令链路：仓库当前没有可依据的协议帧/UUID/固件代码，因此不能凭猜想写。
- ESP32 保存信息与 APP 状态真正双向同步。
- ESP32 时间写入、时间同步失败、临时授权失效的真实硬件联动。
- 唯一管理员席位在 ESP32 侧的真实设备绑定、迁移、抢占和恢复规则。
- 最终 APK 在真实手机上的截图对照与逐项视觉修正。

## Actions/构建要求

- 用户截图显示：**#397 Build APK 为绿色成功**；**#396 为 Cancelled**，原因是同一 main 上存在更高优先级等待请求。Cancelled 不能当成代码错误。
- 每次批量写入后必须等待最新 main 构建完整结束。
- `cancel-in-progress: true` 会在连续提交时主动取消上一轮构建；被取消的运行不能当成代码错误。
- 只有 Analyze / Build / Verify APK 等实际步骤出现红色错误时，才根据 Actions 日志修代码。

## 当前协作方式

- 用户：运行 Actions / 安装 APK / 提供真实错误截图和实机截图；如果有 ESP32 固件、协议文档、service/characteristic UUID 或真实指令帧，直接提供给助手。
- 助手：先审核当前仓库代码、目标记录和实际 Actions，再批量写入；不根据猜想编造硬件协议或错误。
- 优先批量推进：同一轮集中处理 BLE、状态机、权限、借车、日志、计时和 UI 相关项，避免一个按钮一个模块地推进。
