# Tian Key V11 项目进度

> 进度基准继续沿用用户提供的目标与当前仓库实际代码。这里的“代码阶段”只统计仓库已经写入并能被 Actions 验证的内容；最终 APK 完成度必须经过 Actions 成功 + 实机安装截图验收，不能用代码百分比替代实机验收。

## 当前详细进度

| V4目标 | 上一批代码阶段 | 本批次代码阶段 | 状态 | 还缺什么 |
|---|---:|---:|---|---|
| 最终 HUD/UI 框架 | 70% | 80% | 🟡 | 实机截图逐像素验收、状态栏/屏幕尺寸适配 |
| 目标图片视觉风格 | 75% | 82% | 🟡 | 实机截图对照；发现偏差后集中修正 |
| 车辆背景/素材接入 | 100% | 100% | 🟢 | 目标素材已直接作为页面底图；待 APK 验收 |
| 六项车辆控制模拟 | 90% | 95% | 🟢 | 真实 BLE/ESP32 指令链路；7秒动作目前已在 APP 侧形成倒计时状态 |
| 锁车 | 90% | 95% | 🟢 | 真实硬件联调 |
| 解锁 | 90% | 95% | 🟢 | 真实硬件联调 |
| 寻车双脉冲逻辑表达 | 75% | 80% | 🟡 | 真实 GPIO 双脉冲与时序联调 |
| 升窗7秒 | 80% | 90% | 🟢 | 真实 GPIO 保持7秒；APP侧已有7秒状态倒计时 |
| 降窗7秒 | 80% | 90% | 🟢 | 真实 GPIO 保持7秒；APP侧已有7秒状态倒计时 |
| 后备箱7秒 | 80% | 90% | 🟢 | 真实 GPIO 保持7秒；APP侧已有7秒状态倒计时 |
| 管理员初始密码 | 100% | 100% | 🟢 | 固定为 13092991951；实机验证 |
| 管理员认证模拟 | 85% | 88% | 🟢 | 真实 ESP32 持久化认证/设备绑定 |
| 临时借车 | 80% | 88% | 🟢 | 实机 BLE 授权链路 |
| 临时密码有效期 | 80% | 90% | 🟢 | ESP32 时钟/失效联动实机验证；APP侧已增加到期自动清除 |
| 唯一管理员席位 | 70% | 72% | 🟡 | 真正的设备席位写入、迁移、抢占/恢复规则 |

## 总体

- 上一批代码阶段保守评估：**83.0%**
- 本批次代码阶段保守评估：**约 86.0%**
- 本批次对应代码阶段剩余：**约 14.0%**
- **注意：86.0% 仍然不是最终 APK 完成度。** 当前仓库明确仍缺真实 BLE/ESP32 链路与实机视觉验收。

## 本批次一次性推进

1. 保留目标素材作为车辆、借车、设置页面底图，不重新绘制目标图片。
2. 统一车辆六项控制权限门控，继续保持授权关闭时锁定车辆、但不清除当前管理员会话的规则。
3. 六项控制继续使用现有协议/GPIO映射；升窗、降窗、后备箱增加 APP 侧真实7秒倒计时状态，避免只显示一条静态提示。
4. 临时借车有效期增加定时到期处理；到期自动清除临时密码、起止时间，并在借车会话中自动撤销连接状态。
5. 临时借车起始时间改为包含边界，避免刚生成密码时因 `isAfter(start)` 导致第一刻无法使用。
6. 管理员初始密码继续固定为 **13092991951**，没有写入用户此前明确指出的错误密码 **13092991954**。
7. 授权关闭继续只锁定车辆控制，不清除管理员会话；管理员可以直接恢复授权。
8. 自动连接、声音反馈改为真正的本地状态开关并持久化，不再只是显示提示。
9. 日志继续限制在当前 APP 会话内最多200条，避免无限累积；设备状态（密码、设备名、授权、临时密码）继续持久化。
10. 目标图片页面保持固定宽高比，避免因为不同手机宽高比而把目标素材任意拉伸。
11. 明确代码中仍为演示链路的部分，日志不再把模拟 BLE/ESP32 当成已经完成的真实硬件链路。

## 尚未完成的核心工作

- 真实 BLE 扫描/连接/断开，而不是 APP 延时模拟。
- ESP32 协议与真实 GPIO 指令链路。
- ESP32 保存信息与 APP 状态真正双向同步。
- 时间同步失败、过期、借车失效与 ESP32 实机状态联动。
- 唯一管理员席位的真实设备绑定、迁移、抢占和恢复规则。
- 最终 APK 在真实手机上的截图对照与逐项视觉修正。

## Actions/构建要求

- 每次批量写入后必须等待最新 main 构建完整结束。
- `cancel-in-progress: true` 会在连续提交时主动取消上一轮构建；被取消的运行不能当成代码错误。
- 只有 Analyze / Build / Verify APK 等实际步骤出现红色错误时，才根据 Actions 日志修代码。

## 协作方式

- 用户：运行 Actions / 安装 APK / 提供真实错误截图和实机截图。
- 助手：先审核当前仓库代码和实际 Actions，再批量写入，不根据猜想编造错误。
- 优先批量推进：同一轮集中处理 UI、状态机、权限、借车、日志和计时相关项，避免一个按钮一个模块地推进。
